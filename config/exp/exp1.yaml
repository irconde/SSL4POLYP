defaults:
  - base
  - data/sun_full
reporting:
  inputs_subdir: exp1_sun_baselines_sup_vs_ssl
models:
  - model/sup_imnet
  - model/ssl_imnet
protocol:
  finetune: full
  eval_split: test
  early_stop_split: val
  thresholds:
    primary: f1_opt_on_val
    sensitivity: youden_on_val
  export_curves:
    splits: [test]
    points: 200
threshold_policy: f1_opt_on_val
